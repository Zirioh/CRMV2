import React from 'react';
import Layout from '../components/layout/Layout';

const Home: React.FC = () => {
    return (
        <Layout>
            <h1>Welcome to CRM V2</h1>
            <p>This is the main entry point for the CRM V2 application.</p>
        </Layout>
    );
};

export default Home;